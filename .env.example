# AvtoNetGaraža — Chrome Extension
# This extension has no backend and requires no API keys.
# All data is stored in chrome.storage.local on the user's device.
# No environment variables are needed to run or build this extension.
